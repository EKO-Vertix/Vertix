# URL pública del backend (Render). Vite la inyecta en el build de producción,
# así la web desplegada (GitHub Pages, Vercel, Netlify…) sabe a qué API llamar.
VITE_API_URL=https://vertix-dca9.onrender.com
